# API reference

This reference is generated from the docstrings attached to the exported Julia
API. Use `?name` in the Julia REPL to display the same documentation.

## Basis API

```@autodocs
Modules = [QuSpin.Basis]
Private = false
Order = [:module, :type, :function, :macro, :constant]
```

## Operator API

```@autodocs
Modules = [QuSpin.Operators]
Private = false
Order = [:module, :type, :function, :macro, :constant]
```

## Numerical tools

```@autodocs
Modules = [QuSpin.Tools]
Private = false
Order = [:module, :type, :function, :macro, :constant]
```

## Top-level compatibility helpers

The following docstrings are attached to top-level exported bindings and cover
cross-cutting operations shared by multiple basis or operator types.

```@autodocs
Modules = [QuSpin]
Private = false
Order = [:module, :type, :function, :macro, :constant]
```
